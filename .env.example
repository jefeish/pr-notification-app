# The ID of your GitHub App
APP_ID=
WEBHOOK_SECRET=your_webhook_secret_here

# Use `trace` to get verbose logging or `info` to show less
LOG_LEVEL=debug

# Go to https://smee.io/new set this to the URL that you are redirected to.
WEBHOOK_PROXY_URL=

# Email Configuration (required for email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=your_email@gmail.com

# Alternative SMTP providers:
# For Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# For Yahoo:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587

# For custom SMTP server:
# SMTP_HOST=your-smtp-server.com
# SMTP_PORT=587 (or 465 for SSL, 25 for non-secure)

# PR Creator Notification Guarantee - Ensures PR creators always get notified
# Configure fallback emails for when GitHub users don't have public emails
# 
# Priority order when multiple methods are configured:
# 1. User-specific email overrides (highest priority): If EMAIL_OVERRIDE_USERNAME is set for a user, it is always used.
# 2. If no user-specific override, DEFAULT_CREATOR_EMAIL is used if set.
# 3. If neither of the above, CREATOR_EMAIL_DOMAIN is used to generate username@domain emails.
# 4. If none are set, notification will fail for users without public emails.
#
# Method 1: User-specific email overrides (highest priority)
# EMAIL_OVERRIDE_USERNAME=user@company.com
# EMAIL_OVERRIDE_JOHNDOE=john.doe@company.com
#
# Method 2: Default fallback email for all PR creators without public emails
# DEFAULT_CREATOR_EMAIL=pr-notifications@company.com
#
# Method 3: Generate emails using username + domain
# CREATOR_EMAIL_DOMAIN=company.com  # Creates: username@company.com

# Event Configuration - Simple notification preferences
# What types of PR events should trigger notifications?
NOTIFY_PR_LIFECYCLE=true        # opened, closed, reopened  
NOTIFY_PR_REVIEWS=true          # review submitted, dismissed
NOTIFY_PR_COMMENTS=true         # comments on PRs
NOTIFY_CHECK_RESULTS=true       # CI/CD check completions
NOTIFY_PR_UPDATES=false         # synchronize (new commits), edits

# Enable workflow step debugging - posts detailed processing steps as PR comments
DEBUG_WORKFLOW_STEPS=false
